services:
  - type: web
    name: wa-reminder-agent
    env: node
    buildCommand: npm install && PUPPETEER_CACHE_DIR=/opt/render/project/src/.cache/puppeteer npx puppeteer browsers install chrome
    startCommand: node --max-old-space-size=256 src/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PUPPETEER_CACHE_DIR
        value: /opt/render/project/src/.cache/puppeteer
